export interface ClientDTO {
    id: string;
    code: string;
    paterno: string;
    materno: string;
    nombre: string;
}

export interface CreateClientDTO {
    //id: string;
    code: string;
    paterno: string;
    materno: string;
    nombre: string;
}
